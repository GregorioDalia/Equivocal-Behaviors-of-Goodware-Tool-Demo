[project]
name = "toolbehave"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.27.0",
  "typer>=0.12.0",
  "python-dotenv>=1.0.1",
  "matplotlib>=3.8"

]

[project.scripts]
toolbehave = "toolbehave.cli:app"
